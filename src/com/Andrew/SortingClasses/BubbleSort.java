package com.Andrew.SortingClasses;

public class BubbleSort {
    //Need to keep track of each iteration?
    //Build a constructor

    public void BubbleSort(int[] array){
        int iteration = 0;
        boolean isSorted = false;

        do{
           iteration++;

           //Run through the logic here?

        }while(isSorted);
    }
}
